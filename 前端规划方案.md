# 自动部署 Agent Demo —— 前端规划方案

> 本文档是「写代码之前」的前端设计规划，不包含具体实现代码。
> 依据：
> - 需求文档：`../Agent 驱动的软件自动交付部署系统.md`（第四章前端模块、第六章通信、第八章任务状态）
> - 任务模板：`./Trae任务模板-含日志功能.md`（Step 2 前端 4 个子任务 + 三层日志）
> - 后端契约：`./backend/src/deploy_agent/api.py`（9 种 SSE 事件 + 审批恢复 + 心跳）
> - 参考项目范式：`ontology-agent-111/app/src/ontology_agent/demo/gaokao_app.py`（FastAPI + StaticFiles 挂 dist + SSE）
>
> 约束：不新增需求之外的功能；所有错误结构化处理；注释中文；日志走 loguru/console；优化建议与备选方案**待用户确认后再执行**。

---

## 一、设计目标与范围

### 1.1 目标
构建一个 **AI Native 风格** 的单页前端，让用户能够：

1. 用自然语言提交部署任务（如「部署仓库 http://.../xxx.git 的 ctc_jt_1.1.1 分支」）；
2. 实时看到 Agent 的流式文本回复、工具调用状态、构建日志；
3. 在高风险操作（stop / remove / start container、写/删文件、白名单增删）前弹出审批弹窗，支持「批准 / 拒绝」；
4. 看到完整任务状态流转（INIT → GIT_PULL → BUILD_IMAGE → … → SUCCESS / FAILED / REJECTED）；
5. 在出错时能区分是前端问题还是后端问题（日志分层）。

### 1.2 不做的事（避免越界）
- 不做用户登录 / 权限系统（需求未提）；
- 不做历史会话列表 / 持久化（任务模板明确「Demo 砍掉 SQLite 线程映射」）；
- 不做 SSE 自动重连（任务 3.2 明确「不做自动重连，提示即可」）；
- 不做多环境切换（环境由后端 `.env` 决定）；
- 不引入 UI 组件库（任务模板明确「不做 UI 库依赖，纯 CSS 即可」）；
- 不做审批「edit」决策（后端 middleware 明确「edit 不支持，按 reject 处理」）。

---

## 二、技术选型

| 类别 | 选型 | 依据 |
|------|------|------|
| 框架 | **Next.js (App Router) + TypeScript** | 需求文档第四章明确「TypeScript + React」；任务模板 2.1 明确「create-next-app (TS + App Router)」 |
| 样式 | **Tailwind CSS（可选）或纯 CSS** | 任务模板 2.1：「Tailwind 可选，不做 UI 库依赖，纯 CSS 即可」 |
| SSE 解析 | **自研轻量解析器（基于 fetch + ReadableStream）** | 浏览器原生 `EventSource` 不支持 POST 请求体，必须用 fetch 手动解析 |
| BFF 代理 | **Next.js Route Handler `app/api/chat/route.ts`** | 任务模板 2.1 明确方案，避免前端直连后端绕过 CORS |
| 状态管理 | **React 内置 useState/useReducer**（不引入 Redux/Zustand） | 单页 + 单会话，复杂度不需要外部状态库 |
| 构建/部署 | **开发期**：Next.js dev server + local-gateway.mjs 网关；**生产期**：`next build` 出 `dist` 后由后端 StaticFiles 挂载（参考项目范式） | 见第八章部署集成对比 |
| 测试 | **Vitest 或 node 单测**（仅测 SSE 解析纯函数） | 任务模板 2.2 明确「纯函数测试」 |

> **为什么不用 `EventSource`？**
> `EventSource` 只能发 GET 请求、不能带 JSON body。本项目后端 `POST /api/agent/chat` 需要带 `{message, thread_id, decisions}` 请求体，所以必须用 `fetch` + `ReadableStream` 手动按 `\n\n` 切分事件。

---

## 三、后端接口契约（前端必须严格遵守）

> 以下全部从 `backend/src/deploy_agent/api.py` 实际实现提取，不是凭印象。

### 3.1 HTTP 接口

| 方法 | 路径 | 请求体 | 响应 |
|------|------|--------|------|
| POST | `/api/agent/chat` | `{message?, thread_id?, decisions?}` | SSE 流（`text/event-stream`） |
| GET | `/healthz` | 无 | `{status: "ok"}` |

**请求体字段语义**（来自 `api.py` 的 `ChatRequest`）：
- `message: str | None`：**可选**。新对话必须非空（后端会在 `message` 为空且无 `decisions` 时抛 `ValueError`，返回 error 事件）；审批恢复时可不传。
- `thread_id: str | None`：会话标识。缺省时后端生成 `t-{timestamp}`；**审批恢复时必须传回原 thread_id**，否则会创建新会话导致中断点丢失。
- `decisions: list[dict] | None`：审批决策。格式 `[{"type": "approve" | "reject"}]`。后端支持「单条决策广播到多个中断项」（用户只发一条 `approve`/`reject`，Agent 同时调了多个审批工具时会被复制）。

### 3.2 SSE 事件类型（9 种 + 心跳）

| event | 触发时机 | data 关键字段 | 前端处理 |
|-------|---------|--------------|---------|
| `agent_state` | Agent 状态变化 | `thread_id`, `status: "running" \| "awaiting_approval" \| "completed"` | 更新顶部状态条 |
| `message_delta` | LLM 文本增量 | `thread_id`, `text` | 追加到当前 Agent 消息气泡（流式渲染） |
| `tool_call_start` | LLM 决定调用工具 | `thread_id`, `name`, `args`, `call_id`, `hint` | 新增工具卡片（pending 态） |
| `tool_call_end` | 工具执行完成 | `thread_id`, `name`, `result`, `call_id` | 更新工具卡片为成功/失败（看 `result.success`） |
| `task_status` | 工具对应的部署阶段 | `thread_id`, `status`, `tool` | 更新任务状态条当前阶段 |
| `log` | 工具返回的 log 字段切片 | `thread_id`, `tool`, `text`（每 80 字符一条） | 追加到日志区，自动滚到底 |
| `approval_required` | 需要人工审批 | `thread_id`, `action_requests`, `review_configs` | 弹出审批弹窗，禁用发送框 |
| `stream_complete` | 流正常结束 | `thread_id`, `final_result` | 显示最终结果，恢复发送框 |
| `error` | 流异常 | `thread_id`, `message` | 错误提示，恢复发送框 |
| （心跳） | 15 秒无数据 | SSE 注释行 `: keepalive\n\n` | **解析器必须跳过**（不以 `event:` 或 `data:` 开头） |

### 3.3 task_status 状态枚举（来自 `TOOL_STATUS_MAP`）

后端实际映射了 **9 个工具** 的状态（不是需求文档里的 5 个）：

| 工具 | task_status |
|------|-------------|
| `git_pull_code` | `GIT_PULL` |
| `build_docker_image` | `BUILD_IMAGE` |
| `stop_container` | `STOP_CONTAINER` |
| `remove_container` | `REMOVE_CONTAINER` |
| `start_container` | `START_CONTAINER` |
| `check_service_health` | `HEALTH_CHECK` |
| `list_containers` | `LIST_CONTAINERS` |
| `list_images` | `LIST_IMAGES` |
| `check_dockerfile` | `CHECK_DOCKERFILE` |

> ✅ **已决策**：状态条覆盖全部 9 个 task_status（见第七章决策 3）。巡检工具（LIST_CONTAINERS/LIST_IMAGES/CHECK_DOCKERFILE）也会推动状态条前进。

### 3.4 approval_required 事件数据结构

```json
{
  "thread_id": "t-1700000000",
  "action_requests": [
    {
      "name": "stop_container",
      "args": {"container_name": "ontology-graph"},
      "description": "停止容器 ontology-graph"
    }
  ],
  "review_configs": [
    {
      "action_name": "stop_container",
      "allowed_decisions": ["approve", "reject"]
    }
  ]
}
```

前端审批弹窗逐条展示 `action_requests` 的 `description`（已由后端生成中文描述），用户点「批准 / 拒绝」后，把 `decisions` 放进请求体重新 POST `/api/agent/chat`（**同 thread_id，message 为空**）。

### 3.5 错误返回的两种形态（前端必须区分）

1. **SSE `error` 事件**：流级异常（如 `astream` 抛错）。`data.message` 是异常字符串。
2. **`tool_call_end` 的 `result.success=false`**：工具级失败。`result` 是结构化 JSON：
   ```json
   {"success": false, "error_type": "validation_error", "message": "...", ...}
   ```
   前端工具卡片要展示 `error_type` + `message`，并用红叉图标标记。

---

## 四、目录结构

> 对齐任务模板 2.1-2.4 的子任务划分，便于后续按任务推进。

```
agent-deploy-demo/
├── backend/                          # 已存在，后端不动
├── frontend/                         # ← 新增前端工程根
│   ├── app/
│   │   ├── api/
│   │   │   └── chat/
│   │   │       └── route.ts         # BFF 代理（任务 2.1）
│   │   ├── page.tsx                  # 单页主入口（任务 2.3）
│   │   ├── layout.tsx
│   │   └── globals.css
│   ├── components/                   # 组件（任务 2.3）
│   │   ├── MessageList.tsx           # 消息区（用户气泡 + Agent 流式文本）
│   │   ├── ToolCard.tsx              # 工具卡片（pending/success/failed）
│   │   ├── LogPanel.tsx              # 日志区（docker build 输出滚动）
│   │   ├── ApprovalDialog.tsx        # 审批弹窗（任务 2.3 重点）
│   │   ├── TaskStatusBar.tsx         # 状态条（task_status 阶段流转）
│   │   └── Composer.tsx             # 发送框（Enter 发送，发送中禁用）
│   ├── lib/
│   │   ├── types.ts                  # 9 种 SSE 事件类型定义（任务 2.2）
│   │   ├── sse-parser.ts             # SSE 解析器（任务 2.2）
│   │   └── api-client.ts             # 封装 fetch 调用 + onEvent 回调
│   ├── __tests__/
│   │   └── sse-parser.test.ts        # 纯函数测试（任务 2.2）
│   ├── .env.development              # AGENT_API_URL（任务 2.1）
│   ├── package.json
│   └── tsconfig.json
└── 前端规划方案.md                   # 本文档
```

> 注：根据决策 2，**不引入 `local-gateway.mjs` 网关**，BFF 直连后端 8000。

---

## 五、模块设计

### 5.1 `lib/types.ts` —— 事件类型定义（任务 2.2）

定义一个联合类型，覆盖 9 种事件 + 心跳跳过。每个事件类型对应一个 TS interface，字段与第三章表格**完全一致**。关键点：
- `tool_call_end.result` 类型是 `unknown`（后端返回结构化 JSON 字符串，BFF 或解析器 `JSON.parse` 后透传），前端在 `ToolCard` 里再判断 `result.success`；
- `approval_required.action_requests` 是数组，弹窗要循环渲染；
- 心跳行 `: keepalive` 不属于任何事件类型，解析器内部直接丢弃。

### 5.2 `lib/sse-parser.ts` —— SSE 解析器（任务 2.2）

**职责**：从 `fetch` 返回的 `ReadableStream` 逐块解码，按 `\n\n` 切分事件，解析 `event:` 和 `data:` 行，回调 `onEvent(event)`。

**设计要点**（参考任务模板 2.2 提到的 `ai-native/lib/ontology-agent/sse-parser.ts` 思路）：
1. 用 `TextDecoder` 流式解码 UTF-8 字节，维护一个 buffer；
2. 按 `\n\n` 切分完整事件块（不完整的部分留在 buffer 等下一块）；
3. 每个事件块按行解析：
   - `event: xxx` → 事件名
   - `data: {json}` → JSON.parse
   - `: keepalive`（以 `:` 开头）→ 注释行，**跳过**（任务模板 2.2：「遇到无法解析的行 console.warn 并继续，不得中断」）；
4. 解析失败的单行 `console.warn("sse parse skip: ...")` 并继续，不抛错；
5. 不做自动重连（任务 3.2 明确）。

### 5.3 `app/api/chat/route.ts` —— BFF 代理（任务 2.1）

**职责**：接收前端请求 `{message, threadId, decisions}`（驼峰），翻译为后端 `{message, thread_id, decisions}`（下划线），转发到后端 `/api/agent/chat`，把后端 SSE 流原样透传为 `Response`（ReadableStream）。

**设计要点**：
1. 后端地址读 `process.env.AGENT_API_URL`（默认 `http://127.0.0.1:8000`，见决策 2）；
2. 用 Node `fetch` 拿到后端 `ReadableStream`，包装成 Next.js `Response`，`Content-Type: text/event-stream`；
3. 转发失败 / 超时 `console.error` 输出完整错误，返回 502 给前端；
4. **不做** convert-request / persist / interceptor（任务模板明确砍掉）。

> ⚠️ **字段映射**：前端用驼峰 `threadId`，后端要下划线 `thread_id`。BFF 必须做这层转换，否则 422。

### 5.4 `lib/api-client.ts` —— 前端调用封装

封装 `fetch('/api/chat', {method: POST, body})`，返回 `{onEvent, abort}`。组件用 `useEffect` 订阅 `onEvent`，用 `useState` 更新各区域。提供 `abort()` 供用户主动取消（虽然任务模板不要求自动重连，但用户手动取消是基础体验）。

### 5.5 页面组件 `app/page.tsx` + components（任务 2.3）

**状态模型**（单页 useReducer 管理）：
- `messages: Message[]`：消息列表（用户消息 + Agent 流式文本）
- `toolCards: ToolCard[]`：工具卡片列表（按 `call_id` 索引，pending → success/failed）
- `logs: LogLine[]`：日志行数组（docker build 输出）
- `approval: ApprovalData | null`：当前审批弹窗数据（非空则弹窗）
- `taskStatus: string`：当前任务阶段（INIT / GIT_PULL / … / SUCCESS / FAILED / REJECTED）
- `agentState: "idle" | "running" | "awaiting_approval" | "completed" | "error"`
- `threadId: string | null`：当前会话 ID（从首个 `agent_state` 事件取，后续审批恢复复用）
- `sending: boolean`：发送中禁用发送框

**各组件职责**：

| 组件 | 订阅事件 | 行为 |
|------|---------|------|
| `MessageList` | `message_delta` | Agent 文本边收边渲染，最终文本存 state |
| `ToolCard` | `tool_call_start` → `tool_call_end` | start 时插入卡片（pending + 图标 + 参数折叠），end 时按 `result.success` 显示绿勾/红叉，展开看 `error_type`/`message` |
| `LogPanel` | `log` | 追加到滚动容器，自动滚到底，带「清空」按钮 |
| `ApprovalDialog` | `approval_required` | 弹窗逐条列出 `action_requests` 的 `description` + `args`，两个按钮「批准 / 拒绝」；点击后 POST `/api/chat`（同 threadId，message 为空，decisions=`[{type}]`），期间显示「等待审批结果」 |
| `TaskStatusBar` | `task_status` / `agent_state` / `stream_complete` / `error` | 展示当前阶段，终态用颜色区分（SUCCESS 绿 / FAILED 红 / REJECTED 橙） |
| `Composer` | `agent_state` / `stream_complete` / `error` | 输入框 + 发送按钮，Enter 发送，`sending` 时禁用；审批弹窗期间也禁用 |

**交互流程**：
1. **新对话**：用户输入 → POST `/api/chat` `{message}` → 订阅 SSE → 各组件按事件更新 → `stream_complete` 恢复发送框；
2. **审批恢复**：`approval_required` 弹窗 → 用户点「批准」→ POST `/api/chat` `{threadId, decisions:[{type:"approve"}]}` → 继续订阅 SSE → `tool_call_end`（stop_container 成功）→ 可能再次 `approval_required`（remove_container）→ 再批准 → … → `stream_complete`；
3. **错误**：`error` 事件或 `tool_call_end.result.success=false` → 错误提示，恢复发送框；
4. **心跳**：解析器跳过 `: keepalive`，UI 无感。

### 5.6 三层日志落地（对齐任务模板附录）

| 层 | 位置 | 内容 | 实现点 |
|----|------|------|--------|
| ① 后端运行日志 | `backend/logs/deploy_agent.log`（loguru 轮转） | 请求 / SSE 流 / 工具 / 审批 / 异常 | 后端已实现，前端不动 |
| ② 前端日志 | 页面日志区 + 浏览器 console | `log` 事件实时滚动（docker build 输出） + `[系统]` 错误提示 + `console.error/debug` | `LogPanel` + 各组件错误分支 |
| ③ 部署审计 | `backend/logs/deployments/<thread_id>.jsonl` | 部署全链路事件 | 任务 3.2 后端实现，前端不参与 |

**前端日志红线**：
- 前端自身错误（fetch 失败 / SSE 解析异常）`console.error` 输出，并在日志区顶部追加 `[系统] ...` 灰字，方便区分前端 vs 后端问题；
- 审批弹窗状态变化（弹出 / 已批准 / 已拒绝）`console.debug`；
- **禁止** `console.log` 调试残留（任务 3.3 走查要求）；
- **禁止** 打印 `decisions` / `args` 里的密码 token（后端已打码，但前端也不要主动 dump 原始请求体）。

---

## 六、关键交互时序

### 6.1 新对话 + 审批批准完整时序

```text
用户输入 "部署仓库 http://.../xxx.git 的 ctc_jt_1.1.1 分支"
  ↓
Composer POST /api/chat {message}
  ↓
[agent_state running] → 状态条变 running
  ↓
[message_delta × N] → MessageList 流式渲染 Agent 文本
  ↓
[tool_call_start git_pull_code] → ToolCard 插入 pending
  ↓
[tool_call_end git_pull_code result.success=true] → ToolCard 变绿勾
  ↓
[task_status GIT_PULL] → 状态条阶段前进
  ↓
… build_docker_image 同上 …
  ↓
[approval_required action_requests=[stop_container]] → ApprovalDialog 弹窗，Composer 禁用
  ↓
用户点「批准」
  ↓
Composer POST /api/chat {threadId, decisions:[{type:"approve"}]}
  ↓
[tool_call_end stop_container success=true] → ToolCard 绿勾
  ↓
[approval_required remove_container] → 再次弹窗
  ↓
用户点「批准」→ POST /api/chat {threadId, decisions}
  ↓
… start_container 同上 …
  ↓
[task_status HEALTH_CHECK] → check_service_health
  ↓
[stream_complete final_result] → 显示最终摘要，Composer 恢复
[agent_state completed] → 状态条变 completed
```

### 6.2 审批拒绝时序

```text
[approval_required stop_container] → 弹窗
  ↓
用户点「拒绝」
  ↓
POST /api/chat {threadId, decisions:[{type:"reject"}]}
  ↓
后端 middleware 把 reject 转为错误 ToolMessage，LLM 收到后停止后续步骤
  ↓
[message_delta] → Agent 文本说明「用户拒绝，停止部署」
  ↓
[stream_complete] 或 [agent_state completed]
  ↓
状态条变 REJECTED（前端根据 message_delta 或 agent_state 推断）
```

> ⚠️ 后端 `reject` 不会发专门的 `task_status: REJECTED` 事件，状态条的 REJECTED 是前端推断。建议见第九章备选方案 B。

### 6.3 错误时序

```text
[tool_call_end build_docker_image result.success=false error_type=command_failed]
  ↓
ToolCard 变红叉，展开显示 error_type + message
  ↓
[message_delta] → Agent 文本说明失败原因
  ↓
[stream_complete] 或 [error]
  ↓
状态条变 FAILED
```

---

## 七、用户决策记录（已确认）

> 以下决策已于规划阶段与用户对齐，编码时严格遵守。

### 决策 1：前端参考源 —— 用户会补充 ai-native 源码
- 任务模板 2.1-2.4 引用的 `ai-native/lib/ontology-agent/sse-parser.ts`、`ai-native/components/assistant-ui/tool-fallback.tsx`、`ai-native/app/api/ontology-chat/route.ts`、`ai-native/local-gateway.mjs` 在参考项目 `ontology-agent-111` 包里**不存在**。
- **用户决定**：由用户补充 ai-native 前端源码（或其所在仓库路径）后再开始编码。
- **当前状态**：阻塞编码，规划文档先行。源码到位后，任务 2.1-2.4 的「参考其实现方式」才能落地；在此之前只能基于 `gaokao_app.py` 的范式 + 后端契约自研。

### 决策 2：端口方案 —— BFF 直连后端 8000，砍掉网关
- 后端 `.env` 的 `PORT=8000`；测试脚本里的 `8001` 是历史遗留，不作为前端依据。
- **用户决定**：BFF（`app/api/chat/route.ts`）默认 `AGENT_API_URL=http://127.0.0.1:8000`，直连后端，**不引入 local-gateway.mjs 网关**。
- 影响：
  - 目录结构砍掉 `local-gateway.mjs`；
  - 开发期只需启动两个进程：后端 uvicorn(8000) + 前端 dev(3000)；
  - BFF 透传 SSE 时若后端跨域，由后端补 CORS 中间件（参考项目 gaokao_app.py 用了 `CORSMiddleware(allow_origins=["*"])`，本项目 `api.py` 目前没有，需后端单独确认是否补）。

### 决策 3：task_status 状态条覆盖全部 9 个
- **用户决定**：状态条覆盖后端 `TOOL_STATUS_MAP` 全部 9 个状态（GIT_PULL / BUILD_IMAGE / STOP_CONTAINER / REMOVE_CONTAINER / START_CONTAINER / HEALTH_CHECK / LIST_CONTAINERS / LIST_IMAGES / CHECK_DOCKERFILE）。
- 影响：
  - `TaskStatusBar` 组件要支持 9 个阶段图标 + 文案；
  - 巡检工具（list_containers/list_images/check_dockerfile）也会推动状态条前进，前端不再区别对待；
  - 状态条布局要考虑 9 个阶段的横向展示（可能需要换行或滚动）。

### 决策 4：生产部署 —— Next.js 独立进程
- **用户决定**：生产期用 Next.js 独立进程（PM2 或 systemd 守护），BFF 代理到后端 8000，**不让后端 StaticFiles 挂载 dist**。
- 影响：
  - 后端 `api.py` 不需要补 `StaticFiles` 挂载代码（保持现状）；
  - 部署时前后端各自独立进程，通过 BFF 路由 `/api/chat` 转发；
  - 需要解决生产期 CORS（BFF 同源转发可绕过，但若前后端不同域名则后端要加 CORS）。

---

## 八、部署集成方式对比（参考项目 vs 任务模板）

| 维度 | 参考项目范式（gaokao_app.py） | 任务模板范式（local-gateway + Next.js） |
|------|------------------------------|----------------------------------------|
| 开发期 | Next.js dev → 后端 StaticFiles（同源） | Next.js dev(3000) → gateway(9080) → 后端(8000) |
| 生产期 | `next build` → 后端 `StaticFiles(directory=dist, html=True)` 挂载 | 同左（任务模板未明说，但参考项目如此） |
| CORS | 同源不需要 | 网关统一转发，避免 CORS |
| 复杂度 | 低（单进程） | 中（三进程） |
| 适用场景 | 前后端同仓库、单端口交付 | 前后端分离开发、网关层做协议转换 |

> 参考项目 `gaokao_app.py` 第 124-125 行明确：`if os.path.exists(FRONTEND_DIST): app.mount("/", StaticFiles(directory=FRONTEND_DIST, html=True), name="frontend")`。这是生产一体化范式。

---

## 九、优化建议与备选方案（⚠️ 待用户确认后再执行）

> 以下均**不主动执行**，列出供用户选择。

### 备选方案 A：task_status 状态条覆盖范围
- **A1（推荐）**：只展示 5 个核心部署阶段，巡检工具不进状态条；
- **A2**：覆盖全部 9 个 task_status，状态条更全但更长；
- **A3**：状态条按 SOP 顺序固定显示 6 个阶段（INIT + 5 个核心），巡检工具的 task_status 只在 ToolCard 闪烁提示不前进阶段。

### 备选方案 B：REJECTED / FAILED 状态推断
后端 `reject` / 工具失败时不发专门的终态 task_status 事件，前端只能靠 `message_delta` 文本或 `agent_state` 推断。
- **B1（推荐）**：前端在 `approval_required` 后用户点「拒绝」时，本地置 `taskStatus = "REJECTED"`；
- **B2**：等 `stream_complete` 后根据 `tool_call_end` 里是否有 `success=false` 推断 FAILED；
- **B3**：建议后端在 middleware reject 分支补发一个 `task_status: REJECTED` 事件（属于后端改动，需单独确认，不在本前端规划范围内）。

### 备选方案 C：SSE 解析器实现方式
- **C1（推荐）**：自研 fetch + ReadableStream 解析器（任务模板要求）；
- **C2**：用 `@microsoft/fetch-event-source` 库（支持 POST + header，但引入依赖，违反「不做 UI 库依赖」精神，且是解析库不是 UI 库，边界模糊）；
- **C3**：后端改用 WebSocket（需求文档第九章提过「SSE 或 WebSocket」），但后端已实现 SSE，改 WS 属于大改，不推荐。

### 备选方案 D：网关层是否保留
- **D1（推荐）**：开发期保留 local-gateway.mjs（任务模板 2.4 要求，且参考项目有此文件可复制）；
- **D2**：砍掉网关，BFF 直连后端 8000，减少一个进程（但失去网关层的协议转换扩展点）。

### 备选方案 E：样式方案
- **E1（推荐）**：纯 CSS Module + 少量全局 CSS（任务模板「纯 CSS 即可」）；
- **E2**：Tailwind（任务模板「可选」），开发更快但增加依赖；
- **E3**：CSS-in-JS（styled-components），不推荐（运行时开销 + 学习成本）。

### 优化建议清单（非方案选择，是通用建议）
1. **BFF 字段映射加单测**：驼峰转下划线容易写错（threadId → thread_id），加纯函数测试；
2. **审批弹窗去重**：同 `call_id` 只弹一次（任务 3.2 要求）；
3. **日志区上限**：`logs` 数组设上限（如 5000 行），超过丢弃最旧的，防止长构建撑爆内存；
4. **流式文本防抖**：`message_delta` 高频追加时用 `requestAnimationFrame` 批量更新，避免每个 delta 都触发 render；
5. **断流提示**：fetch 的 `ReadableStream` 结束时若没收到 `stream_complete`，提示「连接中断」（任务 3.2 要求「断流重连提示」）；
6. **审批弹窗参数脱敏**：`args` 里可能含 `repo_url`（含 token 已被后端打码）或 `image`，前端展示时不要再二次暴露原始值，直接展示后端给的 `description` 即可。

---

## 十、验证清单（对齐任务模板第二章验证表）

| 任务 | 验证方式 | 通过标准 |
|------|---------|---------|
| 2.1 BFF 代理 | `npm run dev` 后 `curl -N http://localhost:3000/api/chat` | 后端事件穿透到前端端口 |
| 2.2 SSE 解析 | `npm run test` 或 debug 面板 | 9 种事件类型 + 心跳跳过解析正确 |
| 2.3 页面 | 浏览器手动发消息 | 流式文字 / 工具卡片 / 日志滚动 / 审批弹窗全出现 |
| 2.4 联调 | 三次演示（批准 / 拒绝 / 只拉代码） | 三次事件序列与最终状态符合预期 |
| 日志 | 联调时对照三源 | 浏览器 console + 后端 `deploy_agent.log` + `logs/deployments/*.jsonl` 各截一段 |

**联调三次演示话术**（来自任务模板 2.4）：
1. 「部署仓库 http://.../xxx.git 的 ctc_jt_1.1.1 分支」→ 批准；
2. 「部署到容器 not-in-whitelist」→ 拒绝（容器名不在 CONTAINER_NAMES 白名单）；
3. 「只拉代码不部署」→ 验证 Agent 理解能力（只走 git_pull_code，不触发审批）。

---

## 十一、风险与待办

1. **阻塞：等待用户补充 ai-native 前端源码**（见决策 1）。源码到位前，任务 2.1-2.4 的「参考其实现方式」无法落地，只能基于 `gaokao_app.py` 范式 + 后端契约自研。
2. **CORS 待确认**：BFF 直连后端 8000，若前后端同机不同端口（3000 → 8000），浏览器会发 CORS 预检。参考项目 `gaokao_app.py` 用了 `CORSMiddleware(allow_origins=["*"])`，本项目 `api.py` 目前没有。建议后端补 CORS（属后端改动，需单独确认），或前端 BFF 用服务端 fetch 规避（BFF 在 Node 侧请求后端，不走浏览器 CORS）。
3. **InMemorySaver 限制**：后端用内存 checkpoint，进程重启后旧 thread 不可恢复，前端要在 README 说明（任务 3.2 要求）。
4. **生产部署细节**：Next.js 独立进程（决策 4）的 PM2/systemd 配置、端口规划、日志归属等，待开发完成后再定。

---

## 十二、推进顺序建议

按任务模板 Step 2 的 4 个子任务顺序推进，每个任务做完验证通过再进下一个：

1. **任务 2.1**：`create-next-app` 初始化 + BFF 代理（默认连 8000）+ `.env.development`，验证 curl 透传；
2. **任务 2.2**：`lib/types.ts` + `lib/sse-parser.ts` + 纯函数测试，验证 9 种事件解析；
3. **任务 2.3**：`app/page.tsx` + 6 个组件，浏览器手动跑完整流程；
4. **任务 2.4**：双进程联调（后端 8000 + 前端 3000），跑三次演示。

**阻塞前置**：任务 2.1 开工前需用户先补充 ai-native 前端源码（决策 1），否则只能自研。

每个任务完成后运行对应验证命令并汇报结果，失败修复到通过为止。
