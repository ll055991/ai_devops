# Issue Tracker

- **类型**：Local markdown（本机无 `gh` CLI，无法直发 GitHub Issues；有 `gh` 后可迁移到 `ll055991/ai_devops` 的 GitHub Issues）
- **位置**：本仓库 `.scratch/<feature>/spec.md`
- **Triage**：未安装 `triage` skill，无 label 词汇；spec frontmatter 记 `status: ready-for-agent` 表示可开工
